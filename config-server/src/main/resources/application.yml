spring:
 application:
  name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/muzaffersnbl/4.5-PROJECT
          default-label: master
          search-paths: 
           - configuration/
           -configuration/{application}



server:
  port: 8787
        
